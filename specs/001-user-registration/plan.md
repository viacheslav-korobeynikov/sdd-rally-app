# План реализации: Регистрация и аутентификация пользователей

**Ветка**: `1-user-registration` | **Дата**: 2025-12-17 | **Spec**: [spec.md](./spec.md)  
**Входные данные**: Спецификация функции из `/specs/001-user-registration/spec.md`

**Примечание**: Этот документ создан командой `/speckit.plan`. См. `.specify/templates/commands/plan.md` для рабочего процесса выполнения.

## Резюме

Реализация системы регистрации и аутентификации пользователей для платформы организации ралли. Включает регистрацию с логином/паролем, вход в систему, управление сессиями, ролевую модель доступа (RBAC) с 4 ролями, защиту от брутфорса и полное логирование событий безопасности. Технический подход: Golang backend с Fiber фреймворком, PostgreSQL для хранения данных, Templ для серверных шаблонов, HTMX для интерактивности, bcrypt для хеширования паролей, session-based аутентификация с HTTP-only cookies.

## Технический контекст

**Language/Version**: Go 1.21+  
**Primary Dependencies**: Fiber v2 (HTTP фреймворк), pgx v5 (PostgreSQL драйвер), Templ (шаблоны), golang-migrate (миграции БД), zerolog (логирование)  
**Storage**: PostgreSQL 15+  
**Testing**: Go testing package + testify для assertions  
**Target Platform**: Linux server (Docker контейнер)  
**Project Type**: Web application (backend + server-side rendered frontend)  
**Performance Goals**: 
- Регистрация: < 3 секунды на запрос при 100 одновременных регистрациях
- Вход: < 2 секунды в 99% случаев
- API endpoints: < 200ms p95 latency

**Constraints**: 
- Поддержка 1000+ одновременных сессий
- Сессии хранятся в БД (можно мигрировать на Redis позже)
- Bcrypt cost factor 12 (безопасность vs производительность)
- Логирование всех событий безопасности без потери данных

**Scale/Scope**: 
- Начальная оценка: до 1000 пользователей
- До 10000 сессий в день
- Хранение истории попыток входа за 30 дней

## Проверка соответствия конституции

*GATE: Должна пройти перед началом Phase 0 research. Повторная проверка после Phase 1 design.*

### ✅ Принцип I: Архитектура модульного монолита
- **Статус**: СООТВЕТСТВУЕТ
- **Обоснование**: Auth модуль будет иметь чёткие границы (internal/auth/), собственные handler/service/repository слои. Интерфейсы будут определены для взаимодействия с будущими модулями (competitions, crews). Возможно выделение в отдельный auth сервис в будущем.
- **Действия**: Создать internal/auth/ с подпапками: handlers, services, repositories, models

### ✅ Принцип II: Чистая архитектура backend (Golang + Fiber)
- **Статус**: СООТВЕТСТВУЕТ
- **Обоснование**: Используется Golang 1.21+ с Fiber v2. Архитектура строго разделена на слои:
  - **Handler**: HTTP request/response, валидация входных данных
  - **Service**: Бизнес-логика регистрации, аутентификации, управления ролями
  - **Repository**: Работа с БД (users, sessions, login_attempts, role_changes)
- **Middleware**: auth, logging, csrf, rate_limit
- **Действия**: Создать middleware/auth.go, middleware/logging.go, middleware/csrf.go, middleware/rate_limit.go

### ✅ Принцип III: Серверный рендеринг (Templ + HTMX)
- **Статус**: СООТВЕТСТВУЕТ
- **Обоснование**: Формы регистрации и входа будут рендериться на сервере с использованием Templ. HTMX для отображения ошибок валидации без полной перезагрузки страницы. Прогрессивное улучшение - формы работают без JavaScript.
- **Действия**: Создать web/templates/auth/register.templ, web/templates/auth/login.templ, web/static/htmx.min.js

### ✅ Принцип IV: База данных как источник истины (PostgreSQL)
- **Статус**: СООТВЕТСТВУЕТ
- **Обоснование**: Все данные пользователей хранятся в PostgreSQL. Строгая нормализация: таблицы users, sessions, login_attempts, role_changes. Нет бизнес-логики в триггерах. Используется golang-migrate для версионирования схемы.
- **Действия**: Создать миграции 001_create_users.up.sql, 002_create_sessions.up.sql, 003_create_login_attempts.up.sql, 004_create_role_changes.up.sql

### ✅ Принцип V: Аутентификация и авторизация
- **Статус**: СООТВЕТСТВУЕТ
- **Обоснование**: 
  - Пароли хешируются с bcrypt (cost 12)
  - Session-based auth с HTTP-only, Secure cookies
  - 4 роли: chief_organizer, secretary, timing, observer
  - CSRF защита middleware
  - Rate limiting: 5 попыток → captcha, 10 попыток → блокировка 30 мин
  - Все события логируются
- **Действия**: Реализовать password hashing в auth service, session management, role-based middleware

### ✅ Принцип VI: Domain-Driven Design для домена ралли
- **Статус**: СООТВЕТСТВУЕТ
- **Обоснование**: Модель User содержит rally-специфичные роли (chief_organizer, secretary, timing, observer). Единый язык используется в коде. Доменные модели независимы от БД (struct tags только для сериализации).
- **Действия**: Создать internal/auth/models/user.go, role.go с доменной логикой

### ✅ Принцип VII: Наблюдаемость и качество
- **Статус**: СООТВЕТСТВУЕТ
- **Обоснование**: 
  - Структурированное логирование с zerolog (JSON формат)
  - Метрики: регистрации/час, неудачные попытки входа, активные сессии
  - Unit тесты для auth service (хеширование, валидация, rate limiting)
  - Integration тесты для полного flow регистрации и входа
- **Действия**: Настроить zerolog, добавить Prometheus метрики, написать тесты

### ✅ Принцип VIII: Семантическое версионирование
- **Статус**: СООТВЕТСТВУЕТ
- **Обоснование**: Это первая feature (v0.1.0). Будет обновлён CHANGELOG.md после реализации.
- **Действия**: Обновить CHANGELOG.md при завершении

### ✅ Принцип IX: Стандарты безопасности
- **Статус**: СООТВЕТСТВУЕТ
- **Обоснование**:
  - CSRF: middleware с токенами для всех POST/PUT/DELETE
  - XSS: Templ автоматически экранирует HTML
  - SQL injection: pgx с параметризованными запросами
  - CORS: централизованная настройка в main.go
  - Security headers: CSP, X-Frame-Options, X-Content-Type-Options
- **Действия**: Реализовать все middleware безопасности, настроить headers

### 📊 Итоговая оценка
**Статус**: ✅ **ВСЕ ПРОВЕРКИ ПРОЙДЕНЫ**

Нарушений конституции не обнаружено. Функция полностью соответствует всем 9 принципам.

## Структура проекта

### Документация (эта feature)

```text
specs/001-user-registration/
├── plan.md              # Этот файл (вывод команды /speckit.plan)
├── spec.md              # Спецификация требований (уже создана)
├── research.md          # Phase 0 вывод (будет создан)
├── data-model.md        # Phase 1 вывод (будет создан)
├── quickstart.md        # Phase 1 вывод (будет создан)
├── contracts/           # Phase 1 вывод (будет создан)
│   ├── auth-api.yaml   # OpenAPI спецификация auth endpoints
│   └── README.md        # Описание контрактов
└── checklists/          # Чеклисты качества
    └── requirements.md  # Checklist спецификации (уже создан)
```

### Исходный код (корень репозитория)

```text
cmd/
└── server/
    └── main.go         # Точка входа приложения

internal/
├── auth/               # Модуль аутентификации
│   ├── handlers/
│   │   ├── register.go      # POST /api/auth/register
│   │   ├── login.go         # POST /api/auth/login
│   │   ├── logout.go        # POST /api/auth/logout
│   │   └── roles.go         # PATCH /api/users/:id/role
│   ├── services/
│   │   ├── auth_service.go       # Бизнес-логика аутентификации
│   │   ├── password_service.go   # Хеширование и проверка паролей
│   │   ├── session_service.go    # Управление сессиями
│   │   └── role_service.go       # Управление ролями
│   ├── repositories/
│   │   ├── user_repository.go          # CRUD операции для users
│   │   ├── session_repository.go       # CRUD операции для sessions
│   │   └── login_attempt_repository.go # Запись попыток входа
│   └── models/
│       ├── user.go           # Модель пользователя
│       ├── session.go        # Модель сессии
│       ├── role.go           # Enum ролей + permissions
│       └── login_attempt.go  # Модель попытки входа
├── middleware/
│   ├── auth.go          # Проверка аутентификации
│   ├── role.go          # Проверка прав доступа (RBAC)
│   ├── logging.go       # Структурированное логирование
│   ├── csrf.go          # CSRF защита
│   ├── rate_limit.go    # Rate limiting
│   └── security_headers.go  # Security headers (CSP, X-Frame-Options и т.д.)
├── database/
│   ├── postgres.go      # Подключение к PostgreSQL
│   └── migrations/      # Файлы миграций
│       ├── 001_create_users.up.sql
│       ├── 001_create_users.down.sql
│       ├── 002_create_sessions.up.sql
│       ├── 002_create_sessions.down.sql
│       ├── 003_create_login_attempts.up.sql
│       ├── 003_create_login_attempts.down.sql
│       ├── 004_create_role_changes.up.sql
│       └── 004_create_role_changes.down.sql
├── shared/
│   ├── errors/         # Общие типы ошибок
│   ├── validator/      # Валидация данных
│   └── logger/         # Настройка zerolog
└── config/
    └── config.go       # Загрузка конфигурации из .env

web/
├── templates/
│   ├── layouts/
│   │   └── base.templ       # Базовый layout
│   ├── auth/
│   │   ├── register.templ   # Форма регистрации
│   │   ├── login.templ      # Форма входа
│   │   └── errors.templ     # Partial для ошибок
│   └── pages/
│       └── home.templ       # Главная страница (заглушка)
└── static/
    ├── css/
    │   └── styles.css       # Базовые стили
    └── js/
        └── htmx.min.js      # HTMX библиотека

tests/
├── unit/
│   └── auth/
│       ├── password_service_test.go  # Тесты хеширования паролей
│       ├── auth_service_test.go      # Тесты бизнес-логики
│       └── validators_test.go        # Тесты валидации
├── integration/
│   └── auth/
│       ├── registration_flow_test.go  # Полный flow регистрации
│       └── login_flow_test.go         # Полный flow входа
└── fixtures/
    └── users.go            # Тестовые данные пользователей

docker-compose.yml      # Локальное окружение (backend + postgres)
.env.example            # Пример переменных окружения (уже создан)
go.mod                  # Go модуль (будет создан)
go.sum                  # Go зависимости (будет создан)
Makefile                # Команды для разработки (уже создан)
```

**Решение по структуре**: Используется структура веб-приложения (Option 2 из шаблона) с адаптацией под Golang + Templ SSR. Backend код в `internal/` и `cmd/`, frontend шаблоны в `web/`, тесты в `tests/`. Это соответствует принципам чистой архитектуры и модульного монолита.

## Отслеживание сложности

> **Заполняется ТОЛЬКО если Constitution Check имеет нарушения, которые должны быть обоснованы**

*Нарушений не обнаружено. Таблица не требуется.*

---

## Следующие шаги

1. ✅ Phase 0: Исследование технологий → `research.md`
2. ✅ Phase 1: Дизайн данных → `data-model.md`
3. ✅ Phase 1: API контракты → `contracts/`
4. ✅ Phase 1: Быстрый старт → `quickstart.md`
5. ⏳ Phase 2: Разбивка на задачи → `/speckit.tasks` (следующий шаг)

**Готовность**: План одобрен. Переходите к `/speckit.tasks` для создания детального списка задач.

